(function () {
  'use strict';

  angular
    .module('point-blank')
    .factory('search-service', searchService);

  searchService.$inject = ['$http'];

  function searchService ($http) {
  }
})();
